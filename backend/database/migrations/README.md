# Migration Source of Truth

Alembic migrations are stored in `backend/alembic/versions`.

This folder exists to keep the production package structure aligned with the hackathon architecture while preserving the current Alembic runtime configuration.
